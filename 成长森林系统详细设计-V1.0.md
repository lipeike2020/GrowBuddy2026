# 成长森林系统详细设计
版本：V1.1 详细评审稿（文件名保留V1.0以维持原链接）  
日期：2026年9月11日  
适用对象：小学一至三年级学生  
交付性质：产品、交互、数据与实现规格，尚非已开发系统

本稿结合产品初步方案、低年级视觉资产及轻量技术方案，作为下一步原型与开发依据。主导航固定为“今日行动、探索发现、我的成长”；旧稿中四入口及今日新建任务的描述由本稿对应规则替代。原有参考文件保留，不覆盖历史设计。

## 1. 产品结构与使用闭环

孩子在探索发现里安排要做的事，在今日行动里选择、排序、执行和完成，在我的成长里回看记录与收获。首次填写称呼与年级后默认进入今日行动；以后打开链接也进入今日行动。

| 主入口 | 核心问题 | 主要内容 | 角色使用 |
| --- | --- | --- | --- |
| 今日行动 | 现在先做什么？ | 今日任务、顺序、任务选择、完成、专注 | 一位当前伙伴，小幅陪伴 |
| 探索发现 | 想做什么，怎样安排？ | 四类模板、自己的目标与计划、独立任务创建 | 以内容图标为主，无角色对话和大型角色图 |
| 我的成长 | 做过什么，有什么变化？ | 任务、目标计划、专注、收藏、伙伴成长、统计 | 伙伴集中在成长模块，记录优先 |

```mermaid
flowchart LR
  E[探索发现：选择或创建目标与计划] --> T[生成任务]
  T --> D[今日行动：选择、排序、执行]
  D --> F[专注计时与结果记录]
  F --> G[我的成长：记录、统计、收获]
  G --> E
```

无注册登录、家长端、管理端或云端业务数据。一个站点的浏览器存储空间保存一个学生档案。任务奖励可设置，专注时长不直接兑换奖励；完整完成目标获得成就，完整完成计划获得勋章，二者在“成就与勋章”统一查看。

## 2. 导航、页面及返回规则

| 页面标识 | 路由建议 | 页面与用途 |
| --- | --- | --- |
| W01 | /welcome | 首次建档、导入备份 |
| T01 | /today | 今日行动 |
| T02 | /tasks/:id | 任务详情、完成记录 |
| T03 | /focus/:sessionId | 专注计时、暂停恢复 |
| E01 | /explore | 探索发现 |
| E02 | /templates/:id | 模板预览与使用 |
| E03 | /goals/new、/goals/:id/edit | 创建、编辑目标 |
| E04 | /plans/new、/plans/:id/edit | 创建、编辑计划 |
| E05 | /tasks/new、/tasks/:id/edit | 创建、编辑独立任务或任务配置 |
| E06 | /goals/:id、/plans/:id | 目标、计划详情 |
| G01 | /growth | 我的成长 |
| G02 | /growth/records | 任务与行动记录 |
| G03 | /growth/focus | 专注明细 |
| G04 | /growth/collections | 成就、勋章、成长之星 |
| G05 | /growth/companions | 六位伙伴与旅程 |
| G06 | /growth/rewards | 我的奖励：余额、礼品、兑换记录、奖品设置 |
| G07 | /growth/rewards/new、/growth/rewards/:id/edit | 创建、编辑奖品 |
| G08 | /growth/redemptions/:id | 兑换详情、记录领取、取消退款 |
| S01 | /settings | 资料、声音、备份、恢复、已删除项 |

采用Hash路由，上表为逻辑路径。三主页面保留各自滚动位置；主入口切换时读取最新数据。详情返回原来源及筛选条件。创建成功可选择“去今日行动”或“继续安排”；系统不自动开启计时。

底部导航始终使用图标加文字。专注全屏页收起底部导航，提供返回；返回其他页面后应用级计时继续，并显示可返回的小条。浏览器切后台或锁屏才自动暂停。任务创建入口统一归探索发现；今日空状态只提供“去探索发现安排”，不弹出创建表单。

## 3. 今日行动详细设计

### 3.1 页面布局

```text
今日行动                         今天的日期
[当前伙伴小图] 今天完成2件 · 专注15分钟
[正在专注／已暂停的会话入口，仅存在会话时显示]
接下来做什么？                  [选择任务] [调整顺序]
[第一项任务：名称、标准摘要、预计时间、奖励]
[开始专注] [记录完成]
[其余今日任务卡……]
[已完成 2件，默认折叠]
[之前的安排 3件，默认折叠]
────────────────────────────────
今日行动       探索发现       我的成长
```

首屏优先显示1—2项待执行任务，伙伴不占大幅背景。今日简报中的完成数按实际完成日计算，不与“今日安排完成率”混用。未保存、缓存未完成等信息使用简短状态条。

### 3.2 列表归属

| 分组 | 进入条件 | 默认排序 |
| --- | --- | --- |
| 今日待执行 | 安排日期为今天，待完成或进行中，未删除 | 当日手动顺序，其次创建时间 |
| 已完成 | 实际完成日期为今天的任务 | 完成时间倒序 |
| 之前的安排 | 安排日期早于今天，仍待完成或进行中 | 原安排日期升序 |
| 暂停的安排 | 已暂停且尚未结束 | 在选择任务面板中查看，不占日常主列表 |

部分完成、已结束记录当次提交后显示反馈，并在“我的成长”留存。今日可在“今日记录”详情查看这些结果，不把部分完成混入已完成数量。

今天完成以前的任务，移入今日已完成，详情保留原安排日期。正在执行的历史任务通过会话条访问。未来任务不自动出现在今日列表。

### 3.3 选择任务

“选择任务”用于从已经存在的任务中安排今天，不创建新任务。面板提供“之前的安排、接下来7天、已暂停”，卡片展示原日期、类型与预计时间。

选中后执行“安排到今天”：同一任务ID改期，记录原日期和变更；暂停任务同时恢复到暂停前可执行状态。已有当日任务显示“已在今天”，避免重复加入。已完成、部分完成和已结束任务不能再次选择领星。

若任务存在运行计时，先暂停保存再改期。未选中的今日任务保持原安排；本次操作只添加选择项，不把清单当成一次整体覆盖。用户选择不再执行时通过详情明确改期、暂停或结束。

### 3.4 排序与任务卡

“调整顺序”进入显式排序模式，提供上移、下移和完成按钮，拖拽作为辅助。运行会话入口固定，不强行修改任务排序。排序仅影响今天的执行展示，不改变计划规则、奖励或统计日期。

顺序以日期＋任务ID保存；改期加入新日期时排到末尾。已完成列表不参与待办排序。跨标签页修改使用顺序版本检查，冲突时刷新并提示重新调整，不静默覆盖较新顺序。

任务卡展示名称、类别图标、预计分钟、星形图标＋奖励数字、完成标准摘要。标准过长进入详情查看；奖励为0显示“无星星奖励”，仍允许完整完成。非计时任务也保留记录完成入口。

### 3.5 完成与其他操作

点击记录完成 → 展示完成标准 → 选择“完成了”或“做了一部分” → 可选心情与一句记录 → 保存 → 显示结果。

有运行或暂停会话时，提交任务结果同时结束该会话，保存有效时长，再保存任务结果；操作应保持一致性，失败不显示完成成功。倒计时结束只结束计时，不替孩子完成任务。

任务详情提供改期、暂停、结束；名称、标准、奖励等配置编辑跳转探索发现所属编辑页。奖励一经首次开始即锁定。编辑完成或取消后返回原详情。软删除先处理会话，历史事实保留。

## 4. 探索发现详细设计

### 4.1 页面布局

页面顶部四个方向：习惯、兴趣、学习、成长。采用物件图标及短文字，不以伙伴头像充当分类。选中方向后显示“推荐模板、我的安排”两个页签。

| 区域 | 内容 | 操作 |
| --- | --- | --- |
| 推荐模板 | 目标模板、配套计划、预计用时与完成标准 | 查看、使用模板 |
| 我的安排 | 当前方向的目标、所属计划、独立任务 | 查看、编辑、暂停、恢复 |
| 新建入口 | 一个“自己创建”按钮 | 选择目标、计划、独立任务 |

默认展示全部方向的少量精选模板，也可筛选某个方向；不同时堆叠20个成就图标与所有模板。我的安排优先进行中，已完成和归档折叠。搜索按名称匹配即可，首版无需推荐算法。

### 4.2 模板与业务关系

目标回答“我想做到什么”，计划回答“我怎样去做”，任务回答“某天做哪件事”。每个计划属于一个目标，目标可有多个计划；独立任务无需目标或计划。

目标模板包含一个示例目标和可选配套计划。使用模板时预览名称、方向、周期、日期、标准、预计用时、星星数；确认后复制为自己的记录，后续静态模板更新不覆盖它。

单独创建计划时先选择已有目标，或在同一流程创建简短目标。不得静默生成孩子看不懂的空目标。创建独立任务只填写任务信息，省去层级。

### 4.3 表单字段

| 对象 | 必需字段 | 可选与默认字段 | 校验 |
| --- | --- | --- | --- |
| 目标 | 名称、四类方向、完成标准 | 说明、期望日期、成就类别 | 名称1—40字，标准1—200字 |
| 计划 | 所属目标、名称、任务名称、周期、起始日、完成标准 | 结束日、预计用时、奖励数、勋章类型 | 结束日不早于起始日；每周至少选一天 |
| 独立任务 | 名称、方向、安排日期、完成标准 | 预计用时、奖励数、说明 | 日期合法；预计用时如填写为1—60分钟 |
| 奖励数 | 非负整数 | 默认1 | 本稿建议0—99；负数、小数及非数字禁止 |
| 记录 | 无强制字段 | 心情、备注 | 备注建议最多500字 |

字数、0—99及预计用时输入范围为本详细稿实施默认值，可在评审中调整，不回溯修改已结算数据。计时上限与任务实际持续时间不同，长任务允许多段计时。

成就类别从A01—A20选择，计划勋章从M01—M20选择；可按方向给出推荐，采用图标＋名称。图标为复用类别，授予事件关联具体目标或计划，不能单凭选了图标就获得收藏。

### 4.4 生成、更新与暂停

首版周期：每日、每周指定星期、单次。保存计划后生成今天至今天＋6天范围内符合条件的任务，起始日在未来时只生成落入窗口的日期。进入应用、跨日及计划更新后补齐窗口；未使用期间不补发未生成的历史任务。

唯一生成键为计划ID＋原始发生日期。改期不改该键；已删除或结束的实例仍保留键，不重新生成。

修改计划展示“从哪天生效、影响几项任务”。仅更新生效范围内未开始的未来实例；已开始或历史实例保留快照。单任务奖励覆盖不被默认奖励变更覆盖。

暂停计划停止生成；将今天及未来未开始实例置为计划暂停，保留暂停来源。恢复只恢复因该计划暂停的实例，不恢复学生自行暂停、删除或结束的任务；过期暂停实例不自动改为今天。目标暂停采用相同原则作用到活动计划。恢复流程展示具体受影响范围。

确认完成计划获得一次勋章；确认完成目标获得一次成就。存在未结束下级安排时先提示清单：可以继续回顾，或确认结束未开始的后续任务并完成；进行中的任务必须先处理结果，不静默强制完成。已完成目标、计划可查看或归档，不重新打开领取奖励；继续相似安排可复制为新对象。

## 5. 我的成长详细设计

### 5.1 页面结构与信息优先级

合并原成长乐园与我的成长，主页面先看记录，再看陪伴与收藏。无独立乐园导航。

```text
我的成长                              [设置]
[天] [周] [月] [全部]     [上一期] 日期范围 [下一期]
[完成任务] [专注时间] [达成目标] [完成计划]
[概览] [任务] [目标计划] [专注] [收获与伙伴]
当前模块的图表、记录和详情入口
────────────────────────────────
今日行动       探索发现       我的成长
```

手机二级模块可横向滚动，名称完整且保留选中状态；不把五个模块强行压成小按钮。默认“周＋概览”，当天操作后的查看记录链接可以直接定位当天对应模块。进入后记住上次用户主动选择。

概览只显示四项核心数字、简短趋势和最近记录；伙伴摘要与本期收获放在下方。伙伴大图、我的奖励和完整收藏均进入二级页面，避免统计首页变成展示大厅。

### 5.2 统一时间筛选

| 范围 | 定义 | 前后切换 | 图表粒度 |
| --- | --- | --- | --- |
| 天 | 所选日00:00至次日00:00 | 按天切换，不默认进入未来 | 当日记录／会话，不强行画趋势 |
| 周 | 周一00:00至下周一00:00 | 按周 | 每日 |
| 月 | 自然月 | 按月 | 每日 |
| 全部 | 建档至报告生成时 | 不提供前后切换 | 按月，记录分页 |

日期统一档案时区，查询采用左闭右开区间；本期尚未结束时截止当前时间。全部表示所有历史，不是把每天的百分比相加。周月图完整显示周期刻度，未来日期留空不记0次失败。

四类方向筛选适用于任务、专注及目标计划。伙伴开心值没有单一任务方向归属，进入伙伴模块时不应用方向筛选并明确显示“全部方向”。时间范围仍保留。详情返回沿用筛选；导出使用当前筛选并附统计时间。

### 5.3 任务记录与分析

| 指标 | 计算口径 |
| --- | --- |
| 完整完成数 | 实际确认完成时间在区间内的不同任务数 |
| 部分完成数 | 部分完成确认时间在区间内的任务数 |
| 实际行动数 | 区间内有开始、有效专注或结果提交的不同任务数，去重 |
| 安排任务数 | 最终安排日期在区间内、已生成且未取消的任务数 |
| 安排完成率 | 上述安排任务中，截至报告时完整完成的数量／安排任务数 |
| 获得星星 | 区间内任务奖励流水之和，不用任务数乘默认值 |

未开始即结束或删除的任务退出安排分母；已开始后结束、部分完成或完成的任务保留。逾期任务不自动归今天。分母0显示暂无安排。记录列表显示任务名称、当时类别、原／最终安排日、结果日期、结果、实际星星、有效专注时长。

本期安排可能在下一期才完成，因此完成率随迟到完成更新；报告注明“截至生成时间”。完成数与安排完成率不共用一个分母。隐藏任务仍保留统计事实，明细标明已隐藏。

### 5.4 目标与计划

默认展示区间内达成目标数、完成计划数及对应明细。另提供“当前进行中目标／计划”入口，标明这是截至现在的状态，不伪装为历史期末状态。

目标详情：标准、当前状态、关联计划、任务结果、专注累计、达成成就。计划详情：规则快照、实例清单、已完成／部分完成数、按安排口径的完成率、勋章。

目标不默认按已完成任务比例自动达成；标准可能是非数量目标，由学生确认。计划到期不自动获得勋章。首版不生成没有事实依据的“目标完成百分比”。

### 5.5 专注统计

| 指标 | 明确口径 |
| --- | --- |
| 累计时间 | 区间内有效片段时间，排除暂停、作废和异常空档 |
| 专注天数 | 区间内有效时间大于0的日期数 |
| 会话次数 | 结束时间落在区间内且有效时间大于0的非作废会话数 |
| 平均每次 | 上述结束会话的完整有效时间之和／次数 |
| 最长连续 | 区间内连续片段与查询区间交集的最长时长 |
| 倒计时达成率 | 区间内结束的有效倒计时中到点次数／对应次数 |
| 暂停与中断 | 按事件发生时间分别计算主动与自动次数 |
| 分类时间 | 按片段所属会话的任务类别快照分组 |

运行中已保存时间可进入时长，页面注明尚有未结束记录；不进入次数和平均值。跨日会话拆分时间，次数归结束日，连续片段用连续组ID保留跨午夜连续性，周月范围内不能误在午夜断开最长连续记录。

时长先汇总后展示，少于1分钟用文字表达；不用“专注能力分数”。明细显示日期、任务、模式、目标时间、有效时间、结果及暂停原因。

### 5.6 收获与伙伴

收获区包含成就、勋章、成长之星、星星流水与我的奖励入口。成就按目标达成时间，勋章按计划完成时间，成长之星按旅程完成时间纳入所选区间。

同一图标可关联多次不同目标／计划的获得记录，区分“获得次数”和“已收集种类”。区间内获得次数按事件算；全部已收集种类最多20类成就、20类勋章。不将同一事件重复计数。

伙伴模块分两个区域：

- 本期成长：开心值新增、开心日数量、阶段变化、完成旅程，服从时间筛选。
- 当前伙伴：当前选择、当前阶段及阶段进度、今日状态，明确显示“当前”，不随历史筛选伪造过去状态。

全部伙伴终身开心日可累计；本阶段开心值升级后会归零，两者分别命名。今日达到4项显示“今日已达成，日结后增加1点”，日结前不把待结算值计入历史新增。

六位伙伴切换立即改变陪伴展示，当天归属首次完整完成时锁定。一天只计给一个旅程。更换保留进度；三阶段分别10／20／30，旅程60个开心日，结束后生成成长之星。旅程结束后开启新旅程需要明确选择，不覆盖历史。

星星余额属于当前资产，不受历史时间筛选影响；本期获得、兑换扣除、取消退回分别按流水发生时间统计。礼品设置与兑换详见“我的奖励”。

### 5.7 我的奖励：星星兑换礼品

“我的成长”概览提供“我的奖励”二级入口，显示当前可用星星和待领取数量；收获与伙伴模块也可进入同一页面，不新增主导航。奖品为线下兑现的现实礼品或活动，例如一本书、一次亲子出游。学生端负责设置、兑换与领取记录，线下约定和兑现由孩子与家人完成，无账号审批或支付流程。

#### 页面与奖品设置

页面顶部显示“可用星星”，下方为“可兑换、兑换记录、奖品设置”三个页签。兑换记录可筛选全部、待领取、已领取、已取消。管理入口不影响今日执行。

| 字段 | 规则 |
| --- | --- |
| 名称 | 必填，1—40字 |
| 图片 | 可选，本地选择JPEG／PNG／WebP；不填使用内置礼盒占位图 |
| 说明 | 可选，最多500字，可写线下约定 |
| 所需星星 | 必填正整数，建议1—9999；不得为0、负数、小数或非数字 |
| 状态 | 启用／停用，新建默认启用 |
| 版本 | 系统维护，每次修改递增，用于兑换时检查价格变化 |

以上长度与价格上限是实施默认值。图片原文件上限5MB，解码后最长边缩至800px，生成不超过500KB的本地图片；处理失败保留表单并允许换图。图片不上传，不支持远程图片地址。图片保存失败则奖品修改不成功。

支持创建、编辑、停用和重新启用，不提供删除已关联历史兑换的奖品。停用后不再接受新兑换，原有待领取订单仍能领取或取消。编辑名称、图片、说明、价格只影响之后的兑换。

每次兑换一份礼品，不设置购物车、数量、库存或一次性奖品配置。同一启用奖品可再次主动兑换，每次重新确认产生独立记录；重复提交同一次确认不产生新记录。

#### 兑换与状态流转

1. 点击“兑换”，显示名称、图片、说明、所需星星、当前余额和兑换后余额。
2. 确认时读取数据库最新奖品版本、启用状态和余额。若奖品变更，刷新确认内容，让学生再次确认；若停用则阻止提交。
3. 余额不足显示“还差N颗星星”，不扣星、不建立订单。
4. 成功后立即扣星并创建“待领取”记录，提示“已兑换，线下领取后记得记录”。不再额外冻结星星。
5. 实际领取后，学生点击“记录已领取”并确认，保存领取时间，不重复扣星。
6. 待领取时可点击“取消兑换”，确认展示退回的原订单星星数；成功后变为“已取消”，原额退回一次。

状态只允许“待领取→已领取”或“待领取→已取消”。已领取不能取消退款；已取消不能直接恢复，想兑换需重新确认。领取与取消并发时以数据库首次成功提交为准，后一次刷新提示最新状态。不得同时出现领取成功与退款成功。

#### 兑换历史与时间筛选

订单保存兑换时的名称、图片引用、说明、价格及奖品版本快照；图片使用不可变资源，编辑奖品图片不能覆盖旧图片文件。记录展示兑换时间、原扣星数、当前状态、领取或取消时间。奖品停用不影响历史查看。

从我的成长进入时保留天／周／月／全部范围：兑换列表默认按兑换时间筛选，状态筛选表示当前状态，并标注“按兑换日期”。不把今天退款的旧订单误认为今天的新兑换。

奖励统计分别按事件时间计算：任务获得、兑换扣除、取消退回、领取次数。净使用＝兑换扣除－取消退回，可能为负，表示本期退回多于兑换。退款不计入任务获得星星。当前余额＝全部获得＋全部退款－全部兑换，始终标注“当前”，不受历史筛选影响。待领取数量为当前状态数量。

#### 保存、备份与异常

每次确认生成并复用同一requestId，兑换事务用其去重；订单ID关联唯一扣星流水redeem:订单ID。退款使用唯一流水refund:订单ID，退款额只读原订单价格快照。订单状态和流水必须同时成功或同时失败，不能仅靠按钮禁用防重复。

领取事务检查待领取状态并更新领取时间；取消事务检查待领取状态、写退款并更新取消时间。星星不直接改一个余额字段，而由流水校验与计算。礼品价格变化不改变退款数额。

新增奖品、订单、图片资源与奖励流水纳入JSON完整备份；用户图片以带MIME类型的Base64保存，导入校验大小、格式和图片引用。内置图片仍按资源ID恢复。导入整体替换，不能重新执行兑换、发奖或退款；维护期间其他标签页停止写入。

## 6. 通用状态与边界

| 情况 | 页面处理 |
| --- | --- |
| 首次没有任务 | 今日显示去探索发现安排，允许暂不安排 |
| 今日都完成 | 折叠完成列表，温暖提示；不自动布置新任务 |
| 没有统计记录 | 显示暂无记录及去今日行动入口，无虚假百分比 |
| 计划生成失败 | 保留已保存配置，显示尚未生成完成，允许幂等重试 |
| 保存失败 | 保留表单／内存记录，提示尚未保存，禁止展示已发奖 |
| 有其他页运行计时 | 展示已有会话，查看或请求接管 |
| 资源未缓存 | 展示缓存进度或重试；不谎报已离线就绪 |
| 导入错误 | 展示具体问题，当前数据保持不变 |
| 删除上级对象 | 展示关联范围，保护完成事实和已发奖励 |
| 从历史页修改安排 | 跳到探索编辑，保存后返回，历史结果不可追改 |

部分完成是本次任务的终态，需要继续时在探索发现复制为新任务；复制显示新日期与新标准，并不是撤销结果再次领星。

## 7. 数据对象与统计基础

沿用Vue、TypeScript、Vite、Dexie及PWA结构。以下是逻辑表；实现时可按事务和查询合并，但不得丢失独立事实。

| 对象 | 核心字段 | 关键约束 |
| --- | --- | --- |
| profile | id、nickname、grade、createdAt | 单档案 |
| goals | id、category、criteria、status、achievementType、completedAt | 完成一次 |
| plans | goalId、recurrence、startDate、endDate、rewardDefault、version、medalType | 关联有效目标 |
| tasks | id、planId、occurrenceDate、scheduledDate、categorySnapshot、status、rewardOverride、rewardLocked、startedAt、resultAt | 计划＋原始发生日唯一 |
| taskOrder | studentId、date、taskIds、revision | 学生＋日期唯一 |
| taskChanges | taskId、kind、before、after、at | 保存改期及关键配置变化 |
| focusSessions | taskId、categorySnapshot、mode、targetMs、status、effectiveMs、endedAt | 仅一活动控制权 |
| focusSegments | sessionId、continuousGroupId、dateKey、startAt、endAt、effectiveMs | 检查点更新当前片段 |
| focusEvents | sessionId、type、reason、at | 主动暂停与自动暂停分开 |
| completionEvents | taskId、result、at、dateKey、rewardSnapshot | 每任务唯一结果 |
| awards | sourceType、sourceId、assetId、awardedAt | 来源类型＋ID唯一 |
| journeys | companionId、stage、stagePoints、totalHappyDays、startedAt、endedAt | 重开产生新ID |
| dailyBindings | studentId、dateKey、journeyId | 首次完成锁定 |
| dailySettlements | studentId、dateKey、completedCount、journeyId、delta | 每日期唯一 |
| starLedger | businessKey、delta、sourceId、at | 业务键唯一；整数 |
| prizes | id、name、imageId、description、costStars、enabled、version | 价格为正整数；停用保留历史 |
| redemptions | id、requestId、prizeId、prizeVersion、nameSnapshot、imageIdSnapshot、descriptionSnapshot、costSnapshot、status、redeemedAt、claimedAt、cancelledAt | requestId唯一；状态只能单向流转 |
| rewardImages | id、mimeType、blob、createdAt | 不可变本地图片，历史引用保留 |
| settings/runtime | schemaVersion、timezone、ownerId、generation、maintenance | 运行与维护协调 |

记录统一UTC时间戳加业务日期归属，软删除保留deletedAt。本详细稿首版默认上海时区，设置页暂不开放变更；如后续开放，采用次日生效和历史日期固定规则。

静态配置保持六位伙伴、A01—A20、M01—M20、D01—D08及模板ID。备份保存配置快照及ID，不嵌入图片。所有统计由同一组纯函数计算，页面与CSV共享，避免各自解释。

## 8. 本地服务与事务边界

这些是前端函数，不是服务器API。

| 命令／查询 | 输入摘要 | 保存与返回 |
| --- | --- | --- |
| createGoal / createPlan / createTask | 已校验表单、模板版本 | 新对象；计划实例幂等生成 |
| selectTasksForToday | taskIds、today | 同ID改期、恢复标记、追加顺序 |
| reorderTodayTasks | date、taskIds、expectedRevision | 校验集合及版本后保存 |
| startTask / startFocus | taskId、计时配置 | 锁奖励；取得控制权；创建会话 |
| pauseFocus / resumeFocus / finishFocus | sessionId、generation | 更新检查点及状态 |
| completeTask | taskId、result、optionalNote | 结束会话、结果、奖励、当日归属一起保存 |
| completeGoal / completePlan | sourceId、标准确认、剩余安排处理 | 状态与唯一award一起保存 |
| settleDays | today | 按历史日期顺序幂等结算 |
| getGrowthReport | range、category、module | 带口径和生成时间的结果 |
| savePrize / setPrizeEnabled | 奖品表单、expectedVersion | 保存配置及图片引用，检查版本 |
| redeemPrize | prizeId、expectedVersion、requestId | 校验余额与启用状态，快照订单与扣星一起保存 |
| claimRedemption | redemptionId | 仅待领取可转已领取，不再扣星 |
| cancelRedemption | redemptionId | 仅待领取可取消，原价退款与状态一起保存 |
| exportBackup / restoreBackup | 数据版本／已校验文件 | 一致快照／整体事务恢复 |

创建复杂模板时，目标、计划和初始窗口实例尽量在一个本地事务写入。滚动补齐按唯一生成键重试。事务中不等待网络、用户交互或图片加载。

完成任务保存失败则结果与星星均不生效。计时先捕获到提交时刻的有效值，再进入事务；失败保留待重试快照，计时暂停，不继续一边计时一边重试旧快照。

## 9. 专注、离线与维护实现约束

计时使用状态机及单调时间差，显示每秒刷新，每5秒保存检查点；隐藏、暂停和结束尽力立即保存。恢复只取最后成功记录，不补计关闭时间。异常空档停止累计；5秒检查点不是所有异常下的零丢失承诺。

多标签页通过控制权和写入代次拒绝旧页面写入；接管需要旧页暂停，失联时只接续持久化检查点。页面通知只负责同步展示。排序、奖励和恢复均依赖数据库事务，不仅依赖禁用按钮。

PWA缓存三主入口及其必需页面代码、模板与优化图片。全部必需资源就绪才提示离线可用。正式地址使用HTTPS，静态文件更新不上传业务数据。

备份为带版本JSON。导入先验证结构、重复ID、引用关系、金额和时间范围，预览后备份当前数据，再在维护模式整体替换。导入会话转暂停；事务失败保留原档案。其他标签页维护期间不能继续写入。

更新等待所有计时停止且数据保存，再进入维护状态进行版本迁移；数据版本不兼容的旧页面停止写入。CSV采用当前报告口径，转义文本并防止表格公式解释。

## 10. 视觉与素材详细落点

| 位置 | 视觉要求 |
| --- | --- |
| 今日行动 | 暖白底、绿色主要按钮、任务清单占主位；一位小伙伴适度陪伴 |
| 探索发现 | 清楚的四类方向、表单和模板；不放全屏森林、角色轮播或角色对话 |
| 我的成长概览 | 数字、短句、趋势与记录优先；收藏和伙伴位于次级模块 |
| 专注 | 大号计时，安静背景，暂停和结束易找；不弹奖励动画 |
| 收藏详情 | 使用现有成就叶环、勋章圆章与缎带，名称和获得来源可读 |
| 伙伴详情 | 集中使用森林场景、阶段名称、开心值与旅程历史 |

沿用暖白#FFF9EF、正文#263C32、森林绿#286347；正文20px，辅助文字至少16px，按钮高度60px。图标配短文字；不只凭色彩区分完成状态。手机二列统计卡，平板可四列，不压小字号。

已有三张首页视觉图含旧四导航，仅作风格参考；开发必须改为三个入口。尚需制作三入口首页、弱角色探索发现、合并后的成长概览及五个模块、计时与结果页、表单、收藏详情等正式原型，本文件不声称这些图片已完成。

伙伴仅有基础图，剩余12幅阶段图待补齐。现有配饰仅保留为视觉源素材，本次不建设装饰购买、兑换或装备功能。运行图片压缩后接入，原始77MB素材包作为设计源保留。

## 11. 关键业务示例

小禾在探索发现选择学习方向的阅读目标，保存每周工作日15分钟计划，奖励3星。系统生成窗口内任务。今日行动只显示今天应做的阅读，孩子可调整到第一项，或选择另一项已有任务改期到今天。

阅读运行6分钟、暂停2分钟、继续9分钟，结束后确认完成。今日显示新增1个完成任务及15分钟；奖励增加3星。我的成长当天任务记录显示完成，专注记录显示1次、15分钟、最长连续9分钟。计划尚未确认完成，不提前授予勋章。

周日回顾确认整个计划达成，获得一枚M11类勋章；关联目标达成再获得A11类成就。我的成长按各自确认日期记录，不能把本周全部任务完成直接当作目标达成。

若某日完整完成4个任务，不论总星星多少，该日结算增加1点开心值。查看本周时显示本周新增开心值；当前伙伴阶段进度单独展示。

## 12. 验收用例

| 编号 | 场景 | 验收结果 |
| --- | --- | --- |
| IA01 | 首次、再次进入 | 建档后默认今日；三个主入口，无独立成长乐园 |
| TD01 | 今日无任务 | 引导探索安排，不出现本页新建表单 |
| TD02 | 选择未来任务到今天 | 原ID改期，不复制；计划实例不重复生成 |
| TD03 | 排序、刷新、双页冲突 | 顺序保存；版本冲突提示刷新 |
| TD04 | 完成历史任务 | 归今日完成数，保留原安排日期 |
| EX01 | 四方向浏览 | 内容与物件图标为主，无强角色依赖 |
| EX02 | 模板与自己创建 | 能创建目标、所属计划及无层级独立任务 |
| EX03 | 重复进入、改期、删除实例 | 生成键稳定，不补发同一任务 |
| EX04 | 计划暂停再恢复 | 学生自行暂停任务不被误恢复 |
| RW01 | 默认3、覆盖5、锁定、0星 | 分别正确结算，0星仍计完成 |
| RW02 | 完成连点及恢复删除 | 结果及流水只产生一次 |
| FC01 | 6＋暂停2＋9分钟 | 15分钟、1次、最长连续9分钟 |
| FC02 | 跨页、后台、锁屏、接管 | 站内跨页保留；隐藏暂停；无重复计时 |
| FC03 | 运行会话时提交结果 | 会话与结果一致保存，失败可重试 |
| GR01 | 日周月全部切换 | 时间范围正确，全部不累加百分比 |
| GR02 | 跨日会话 | 时间按日期，次数按结束日；连续性保留 |
| GR03 | 迟到完成与安排完成率 | 完成日与安排日口径分开，分母0不显示100% |
| GR04 | 成就勋章与种类 | 来源去重，种类和获得次数分别显示 |
| GR05 | 历史筛选与当前状态 | 本期新增服从范围；当前余额／伙伴明确标注 |
| GR06 | 伙伴日结与切换 | 每天一次、旅程正确，升级分别10／20／30 |
| DT01 | 离线进入三个模块 | 管理、计时、统计与备份均可用 |
| DT02 | 错误导入、迁移失败 | 原档案保持；旧页停止不兼容写入 |
| RP01 | 设置、修改、停用礼品 | 必填与价格校验正确；停用阻止新兑换，旧记录可处理 |
| RP02 | 余额不足、重复确认、双页同时兑换 | 不超扣；同requestId只建一单；余额不为负 |
| RP03 | 兑换后改名、改价、换图 | 历史快照不变；取消退原价 |
| RP04 | 领取与取消同时提交 | 只成功一个；已领取不可退款；同单退款一次 |
| RP05 | 跨期退款与统计 | 退款归取消日，不计任务获得；当前余额一致 |
| RP06 | 含礼品图片的备份恢复 | 图片、订单、状态、流水一致，不重发奖或重复退款 |
| UI01 | 360px手机、平板触摸 | 无横向溢出，正文和按钮可读可点 |

优先自动测试日期聚合、奖励、生成去重、事务恢复和计时状态机；锁屏、触摸、浏览器异常与离线更新在真实设备验证。

## 13. 开发拆分与评审默认项

第一步完成三入口壳、探索创建独立任务、今日执行、计时、完成、成长日统计及备份，形成真实数据闭环。第二步接目标计划、模板与生成，再接收藏和伙伴日结。第三步补齐统计筛选、礼品设置与兑换领取退款、素材适配和离线更新验收。

本稿将首页方案2的清单结构作为排版参考，不要求逐像素沿用旧图。奖励0—99、固定上海时区、默认周概览、选择已有任务即改期到今天为明确实施默认项。用户确认的三入口分工与成长合并是本次设计约束，其余默认项可在详细评审中调整。

参考文件：
- [产品初步方案](./成长森林学生端产品初步方案-V2.0.md)
- [低年级视觉设计说明](./视觉设计/低年级视觉设计说明.md)
- [轻量技术实现方案](./成长森林轻量技术实现方案-V1.0.md)
