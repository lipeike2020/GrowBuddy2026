# 成长森林开发准备与素材缺口清单

日期：2026年9月11日  
核对依据：系统详细设计V1.1（文件名V1.0）、产品初步方案、轻量技术方案、视觉资产清单。  
本次仅梳理准备工作，未启动应用开发或新增图像制作。

## 1. 开工判断

可以开始开发，不必等所有插画完成。已有三入口业务分工、任务与专注规则、成长统计、礼品兑换和本地技术路径，足够建立工程、数据库及核心操作闭环。

现有57张图是视觉素材与首页探索稿，不是完整页面原型，也不是运行资源包。完整发布还需要补齐页面状态、角色阶段和内容模板。优先完成“探索创建任务—今日执行与计时—成长查看—备份恢复”，奖励事务模型同时设计，避免后加退款时重构流水。

## 2. 已有材料及可用程度

| 材料 | 当前具备 | 开发使用方式 |
| --- | --- | --- |
| 详细设计 | 三主入口、规则、数据对象、验收场景、我的奖励 | 主需求依据；先解决下述歧义 |
| 技术方案 | 纯前端、本地数据库、离线及事务方向 | 作为架构参考，旧四入口内容不再采用 |
| 伙伴 | 6张基础形态，1254×1254透明PNG | 可以接入基础陪伴 |
| 成就 | A01—A20，20张透明PNG及类别名称 | 接入目标成就，检查小尺寸辨识度 |
| 勋章 | M01—M20，20张透明PNG及计划示例 | 接入计划收藏，不需要重做20张 |
| 装饰 | 6件配饰／摆件／相框、2张背景 | 装饰兑换已取消；2背景可用，其余按需作视觉元素 |
| 页面稿 | 3张旧四导航首页方向稿 | 参考色彩和清单布局，不能照搬导航与入口 |
| 资产说明 | 文件ID、尺寸、透明通道、提示词 | 用于素材版本与运行映射 |

资产清单记录57张独立图片、52张透明背景；本次按清单核对，不是对每张图重新进行视觉验收。约77MB压缩素材包需在运行前做多尺寸优化。

## 3. 开工前必须收敛的规则

这些主要是开发规格补充，不需要重新讨论已经确认的三入口或礼品兑换方向。

| 问题 | 当前缺口／风险 | 建议处理 | 负责人 |
| --- | --- | --- | --- |
| 文档优先级 | 初步方案和技术方案仍残留四入口、装饰、旧时间估算 | 详细设计作为唯一需求基线；旧稿标记参考，列出冻结配置 | 产品＋开发 |
| 备份图片 | 详细设计5.7要求包含礼品图片，数据章节又写“不嵌入图片” | 明确：内置资源只存ID；用户礼品图片必须随备份保存 | 开发可直接统一 |
| 开始任务 | UI强调开始专注／记录完成，技术有startTask，但非计时开始入口未明确 | 任务详情提供“开始任务”；直接完成在提交时锁奖励 | 产品＋交互 |
| 暂停会话数量 | 只允许一段运行，但是否允许多段未结束暂停会话不够清楚 | 首版仅一段未结束会话；启动另一任务先结束并保存旧段 | 产品＋开发 |
| 计划暂停后的选择 | 今日选择暂停任务可能绕过仍暂停的上级计划 | 单次恢复本任务明确提示，不恢复计划或生成其他任务；记录独立恢复原因 | 产品＋开发 |
| 计划改周期 | 已生成但不再符合新周期的未来任务处理未完全明确 | 未开始且在生效范围的旧实例取消，保留生成键；新日期幂等生成；单独改期实例保留并提示 | 产品＋开发 |
| 旅程结束 | 旅程完成后，未选新旅程就完成任务时归属不明 | 开始新一天执行前提供明确旅程选择；若跳过，任务照常完成，当日成长不计入已结束旅程，文案说明 | 产品确认 |
| 奖励确认重试 | requestId提到复用，但刷新后的未决请求未说明 | 确认意图ID本地保留，结果不明先查订单，不能刷新后自动生成新请求再扣星 | 开发 |
| 时间错误 | 时钟回拨可能影响已结算日与统计 | 已结算日期不重发；检测异常时提示校准，不自动回写历史 | 开发＋测试 |
| 删除与恢复 | 目标、计划与任务级联恢复规则未完全收敛 | 记录删除来源；恢复上级不自动恢复此前单独删除的子项，不补领任何奖励 | 开发 |
| 设置与重置 | 详细稿只列设置入口，未完全展开 | 继承产品稿的资料、声音、动效、备份、已删除项、重置保护；形成页面状态清单 | 交互＋开发 |

奖励0—99、礼品1—9999、固定上海时区、默认周概览、窗口今天至＋6天属于现有实施默认值。可以先按这些开发并集中配置，不必每一项再次询问。上述旅程归属等会影响历史数据的规则应在相关模块编码前收敛。

特别注意：无登录的本地应用允许学生修改奖品与奖励，这是既定产品模式。不要擅自添加家长密码、审核或服务器防作弊来解决产品并未提出的问题。

## 4. 缺失素材：必须补什么

| 编号 | 素材 | 数量／范围 | 优先级 | 可否先替代 |
| --- | --- | --- | --- | --- |
| V01 | 少年期、成熟期伙伴图 | 6位×2阶段＝12张 | 完整成长发布前 | 内测用基础图＋阶段文字；不能视作最终形态交付 |
| V02 | 伙伴成长之星 | 6枚，各保留对应伙伴特征 | 旅程模块发布前 | 临时统一星形＋伙伴头像 |
| V03 | 礼品默认礼盒图 | 1张，无文字，适合正方形卡片 | 奖励页面开发时 | 简单SVG占位可由开发制作 |
| V04 | 应用标识 | 1套应用图标及favicon、安装图标输出 | 发布前 | 选定团团头像作为临时标识，不新增第七种动物 |
| V05 | 基础功能图标 | 三导航、四方向、排序、计时、暂停、完成、设置、礼品、导入导出等约20—30种 | 核心页面开发时 | 采用统一图标集或代码原生SVG |
| V06 | 心情符号 | 开心、平静、疲惫、难过等约4—6个 | 记录模块开发时 | 简单符号＋文字；不要依赖不同系统的Emoji外观一致 |
| V07 | 空状态与成功反馈 | 无任务、无记录、无礼品、保存成功等 | 可边开发边补 | 通用图标＋短文案，无需每种独立插画 |

五档每日状态尚无专用图。建议首版用同一角色图＋状态文字／小标识完成，不要求绘制6×3×5共90张状态图。若未来需要表情动画，再单独设计。

不需要补齐或继续制作装饰商城、装饰价格、穿戴锚点、装备态或购买按钮；装饰兑换已取消。现实礼品图片由用户自行选择，不需要预先制作20张礼品照片。

## 5. 缺失的页面设计与交互状态

优先补主流程的可点击线框，再制作少量代表页面的高保真稿。页面不必全部通过生图完成；真实按钮、表单和图表应使用可复用组件。

| 批次 | 页面／流程 | 必须覆盖的状态 |
| --- | --- | --- |
| P0 | 三入口导航与今日行动 | 无任务、有任务、运行／暂停、已完成、历史安排、选择任务、排序 |
| P0 | 探索发现与创建独立任务 | 四方向、模板、我的安排、创建成功、字段错误、未保存退出 |
| P0 | 专注与任务结果 | 模式选择、运行、暂停、到点、提前结束、中断恢复、完整／部分完成 |
| P0 | 我的成长概览 | 日周月全部、零记录、当前与历史区分、记录详情 |
| P0 | 欢迎与备份恢复 | 新建、已有档案、导入预览、错误文件、覆盖确认 |
| P1 | 目标与计划 | 模板预览、创建、编辑生效范围、暂停恢复、到期回顾、达成确认 |
| P1 | 成长各模块 | 任务、目标计划、专注、收获伙伴；各自筛选与明细 |
| P1 | 我的奖励 | 可兑换、奖品设置、编辑图片、停用、余额不足、价格变更再确认 |
| P1 | 兑换订单 | 待领取、领取确认、取消退款、已领取、已取消、历史快照 |
| P1 | 伙伴与收藏 | 六位选择、阶段进度、日结待生效、旅程结束、新旅程、成就勋章详情 |
| P1 | 设置 | 资料、声音动效、存储状态、已删除项、备份、重置、更新 |

P0表示第一批闭环所需，P1表示相关完整模块发布前所需，不表示可从正式范围删除。

建议先以360px手机和约834px平板制作代表页面；电脑延用响应式布局。至少形成一份组件规范：字号、间距、颜色、按钮、输入框、弹窗、卡片、导航、统计卡及禁用／加载／错误状态。当前视觉说明有基础数值，但还没有完整组件稿。

## 6. 内容配置也是素材

当前有图标类别和简短模板示例，但还没有可直接导入程序的完整内容库。

| 内容 | 建议首版准备 | 交付格式 |
| --- | --- | --- |
| 四方向目标计划模板 | 每类3套，共12套；数量为建议 | JSON或可转换表格 |
| 模板字段 | ID、名称、方向、目标标准、计划标准、任务标准、周期、预计时间、奖励默认、A/M图标关联 | 稳定字段结构 |
| 伙伴资料 | 名称、物种、三阶段名、图片映射、阶段阈值、简短提示语 | JSON |
| 收藏配置 | 20成就＋20勋章的ID、名称、说明、图像路径 | JSON |
| 全局文案 | 操作、保存失败、备份提醒、退款、时长中断、无记录 | 文案表 |
| 演示数据 | 阅读、习惯、0星任务、跨日专注、旧订单改价与退款等 | 独立测试数据包 |

模板的目标标准与计划标准需能由孩子理解并自查，不能仅写“更优秀”。奖励默认不强制随年级增长；数字为示例可修改。首版礼品库建议为空，提示孩子与家人商量后创建；不要预置看似平台承诺兑现的奖品。

## 7. 开发工程准备

由开发完成，不需要用户逐项提供：

- 创建单工程及版本管理，锁定依赖；开发、预览与生产分别配置。
- 确立三入口路由和组件边界；业务规则、数据库及展示分层。
- 完成数据库具体schema、索引、唯一键与事务设计；数据版本从第一版建立。
- 明确启动顺序：检查存储→读取档案→恢复中断→补算日结→补齐任务→展示页面，避免未结算旅程先接收新日任务。
- 建立奖励状态机、计划实例生成、暂停来源、删除恢复、日期统计和计时控制的测试夹具。
- 建立图片运行目录与manifest；原图保留，输出多尺寸WebP及必要回退。不要把3张页面稿和全部原图打入首屏。
- 制定用户图片解码、尺寸／体积限制与失败回退；保留订单引用的旧图，禁止清理仍被历史记录引用的图片。
- 定义JSON备份schema、支持版本、总体文件与记录数限制；不只检查单张图片5MB。内测用含图片的大备份验证内存占用。
- 计时跨标签接管、维护锁、刷新中断、导入原子性尽早验证，避免临近发布才发现设备差异。
- 日志留本地且避免记录奖品图片和完整个人备注，不默认接入第三方追踪服务。

仍需用户侧准备：产品最终名称／图标偏好、用于验收的真实设备、验收负责人，以及正式发布时的域名与托管使用权限。正式域名不阻挡本地开发；不需要先购买业务服务器、云数据库或短信服务。

## 8. 测试与上线准备

### 第一批测试样本

准备一个全新档案、一个使用一周的档案、一个跨月档案和一个大数据档案。覆盖0星、奖励覆盖、部分完成、改期、暂停计划、伙伴阶段边界、待领取／已领取／取消礼品及用户图片。

按详细稿验收编号建立测试表，额外覆盖：旅程第60个开心日后的首次任务、暂停计划中单任务恢复、改周期后的已生成实例、退款发生在兑换次月、备份图片恢复、兑换结果不明后的重试。

### 真实设备

至少一台Android手机、一台iPhone或iPad、一台Windows电脑进行测试；如目标家庭主要使用某类设备，优先覆盖该类。验证触摸排序替代按钮、锁屏暂停、系统回收、离线重开、文件下载／选择、存储空间不足和安装模式。浏览器版本与测试日期记录在验收表，不只凭桌面模拟器判断。

### 发布交付

准备稳定HTTPS地址、部署和回滚步骤、离线缓存检查、数据库迁移失败处理、首次使用说明及备份说明。旧代码回滚必须兼容当前数据库版本，不把清空本地记录作为升级修复办法。

选择少量家庭试用，重点观察孩子是否能自己找到“安排任务、开始专注、查看记录”和“兑换后记录领取”；用观察修正文案和布局。不要仅以页面能打开作为低年级可用性验收。

## 9. 推荐执行顺序

1. 将详细设计冻结为开发基线，修正文档冲突；收敛会话、计划变更和旅程边界规则。
2. 补P0线框与基础组件规范，建立12套模板内容草稿。
3. 创建工程、数据库与核心闭环；占位图允许使用现有6位伙伴。
4. 开发目标计划、成长和奖励时，同步制作P1页面及12幅阶段图、6枚成长之星。
5. 优化资源、完成备份恢复和真实设备测试，再准备正式部署。

最少新增业务插画是12幅阶段图＋6枚成长之星；默认礼盒和应用图标各一套也需有可用版本。其余大量功能图标和页面状态可以用组件及SVG完成，不需要把所有缺口都转化为新插画。

## 10. 对已有排期的影响

此前20—30人日估算早于此次完整礼品流程及页面整合，不能直接作为当前交付承诺。应按最新页面、状态与验收清单重新拆分；特别计入用户图片与备份、退款并发、计划周期更新和真实设备测试。新增插画制作与开发可并行，但正式三阶段交付需等素材齐备。

当前最有价值的下一项交付是“P0可点击原型＋工程初始化＋数据库规则测试”，而不是继续批量生成装饰图片。

